name: komodo_coin_updates
description: Runtime coin config update coin updates.
version: 1.1.1
repository: https://github.com/KomodoPlatform/komodo-defi-sdk-flutter

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.35.0"

resolution: workspace

# Add regular dependencies here.
dependencies:
  decimal: ^3.2.1
  # required to load build_config.json via AssetBundle from komodo_defi_framework
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  freezed_annotation: ^3.0.0
  hive_ce: ^2.2.3+ce
  hive_ce_flutter: ^2.2.3+ce
  http: ^1.4.0
  json_annotation: ^4.9.0
  komodo_defi_types: ^0.3.2+1
  logging: ^1.3.0

dev_dependencies:
  build_runner: ^2.4.14
  flutter_test:
    sdk: flutter
  freezed: ^3.0.4
  hive_ce_generator: ^1.9.3
  hive_test: ^1.0.1
  index_generator: ^4.0.1
  json_serializable: ^6.7.1
  lints: ^6.0.0
  mocktail: ^1.0.4
  test: ^1.25.7
  very_good_analysis: ^9.0.0
